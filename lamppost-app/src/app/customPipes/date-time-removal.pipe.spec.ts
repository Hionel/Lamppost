import { DateTimeRemovalPipe } from './date-time-removal.pipe';

describe('DateTimeRemovalPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeRemovalPipe();
    expect(pipe).toBeTruthy();
  });
});
